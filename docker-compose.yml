services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "4173:4173"
    env_file:
      - ./backend/.env
    volumes:
      # Keep the database persistent across container restarts
      - spotler_db:/app/backend/data

volumes:
  spotler_db:
